x pages
  x localized fullpath (rebuild the fullpath for localized pages based on the slug / parent slug)

x snippets

x content types

x content entries

x yaml front matter

- haml -> liquid

- theme_assets

- content_assets

- writers
  - API
  - FileSystem
    x site
    x pages
    x snippets
    x content types
    x content entries
    - assets
    - convert in HAML, SASS, Coffeescript


====== use case =====

-- filesystem -> mongodb (mongoid ?)