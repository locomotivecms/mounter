- readers
  - API
    x site
    - pages
      x fetch all (+ translations)
      x editable elements
        x content assets
      - content type (engine does not return the content_type_id)
    x snippets
    x content types
    x content entries
      - content assets
    x theme assets
    x content assets
  - filesystem
    x pages
      x localized fullpath (rebuild the fullpath for localized pages based on the slug / parent slug)
      x editable elements
      x content type
    x snippets
    x content types
    x content entries
    x yaml front matter
    x haml -> liquid
    - theme_assets
      - tests
    - content_assets
      - tests

- writers
  - API
    - site
    - pages
    - snippets
    - content types
    - content entries
  - FileSystem
    x site
    - pages
      - page elements (YAML matters)
    x snippets
    x content types
    x content entries
    x theme assets
      x use url instead of source
    x convert in HAML, SASS, Coffeescript
    x content assets
    - tests

====== use case =====

-- filesystem -> mongodb (mongoid ?)